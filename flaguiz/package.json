{
  "name": "flaguiz-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:routes": "node scripts/build-static-routes.mjs",
    "validate:data": "node scripts/validate-data.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run build:routes && npm run validate:data && npm test"
  }
}
